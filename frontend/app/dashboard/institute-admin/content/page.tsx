"use client";

import { ReusableContentWorkspace } from "@/components/content/ReusableContentWorkspace";

export default function InstituteAdminReusableContentPage() {
  return (
    <ReusableContentWorkspace
      badge="Institute Management"
      title="Reusable Content Library"
      description="Build reusable module content once for your institute, then allocate it to the right batch or selected students whenever that subcourse is delivered."
    />
  );
}
