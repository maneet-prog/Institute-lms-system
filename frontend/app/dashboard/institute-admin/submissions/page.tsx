import { SubmissionReviewWorkspace } from "@/components/submissions/SubmissionReviewWorkspace";

export default function InstituteAdminSubmissionsPage() {
  return (
    <SubmissionReviewWorkspace
      badge="Institute Admin Review"
      title="Student submissions and grading"
      description="Review learner quiz attempts, activity uploads, and grading outcomes for the institute batches you manage."
    />
  );
}
